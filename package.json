{
  "name": "canonical-quest",
  "version": "1.0.0",
  "description": "solution to the canonical code test",
  "main": "index.js",
  "author": "Tochi Bedford",
  "license": "MIT",
  "dependencies": {
    "live-server": "^1.2.2",
    "sass": "^1.54.3",
    "vanilla-framework": "^3.6.1"
  },
  "scripts": {
    "start": "yarn build-css && live-server",
    "build-css": "sass --load-path=node_modules src:dist --style=compressed",
    "watch-css": "yarn build-css && sass --load-path=node_modules -w src:dist --style=compressed"
  }
}
